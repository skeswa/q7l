import React, { StatelessComponent } from 'react'
import * as styles from './index.css'

const StudentApp: StatelessComponent = props =>
  <div className={styles.main}>This is the student app</div>

export default StudentApp
