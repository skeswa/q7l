import React, { StatelessComponent } from 'react'
import * as styles from './index.css'

const RegisterPage: StatelessComponent = () =>
  <div className={styles.main}>This is the register page</div>

export default RegisterPage
