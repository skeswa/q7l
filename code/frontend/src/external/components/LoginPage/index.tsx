import React, { StatelessComponent } from 'react'
import * as styles from './index.css'

const LoginPage: StatelessComponent = () =>
  <div className={styles.main}>This is the login page</div>

export default LoginPage
